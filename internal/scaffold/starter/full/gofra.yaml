# Configuration overrides. Proto defaults apply when a key is omitted here.
# See proto/__GOFRA_PROTO_PACKAGE__/config/v1/config.proto for the full schema.
app:
  name: __GOFRA_APP_NAME__
  port: 3000

database:
  dsn: "postgres://postgres:postgres@localhost:5432/__GOFRA_APP_NAME__?sslmode=disable"
  auto_migrate: true
